# Etat Terraform : contient les secrets en clair
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log

# Variables locales (jetons, mots de passe)
*.auto.tfvars
terraform.tfvars

# Mot de passe Ansible Vault
.vault-pass
